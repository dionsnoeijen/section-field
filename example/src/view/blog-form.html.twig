{{ form_start(form) }}
<div class="row">
    <div class="col-md-12">
        {{ form_errors(form) }}
    </div>
</div>
<div class="row">
    <div class="col-md-10">
        {{ form_row(form.title) }}
        {{ form_row(form.subtitle) }}
        {{ form_row(form.body) }}
        {{ form_row(form.save) }}
    </div>
    <div class="col-md-2">
        {#{{ form_row(form.blogSlug) }}#}
    </div>
</div>
{{ form_end(form) }}
